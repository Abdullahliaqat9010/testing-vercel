export interface ProfessionalAccountItem {
  id: number,
  tag: string,
  img: string,
  activeImg: string,
  name: string
}

export interface SelectItem {
  id: number,
  name?: string,
  label: string,
}